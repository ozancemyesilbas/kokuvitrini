export default function ProductVisual({product,large=false}){return <div className={`product-stage ${large?'large':''}`}>{product.image?<img className="product-photo" src={product.image} alt={product.imageAlt||`${product.brand} ${product.name} ${product.size}`} width={large?900:500} height={large?900:500} loading={large?'eager':'lazy'}/>:<><div className="product-shadow"/><div className="product-bottle" style={{'--tone':product.color}}><span className="bottle-brand">{product.brand}</span><strong>{product.name}</strong><small>EAU DE PARFUM</small></div></>}</div>}
